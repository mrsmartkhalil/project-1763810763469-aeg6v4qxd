# 🚀 دليل البدء السريع - موقع المحامي أحمد

## 📌 خطوات البدء

### 1️⃣ تشغيل المشروع

المشروع جاهز للتشغيل! قم بتنفيذ:

```bash
npm run dev
```

الموقع سيعمل على المنفذ الذي يحدده النظام تلقائياً.

---

## 🔑 معلومات تسجيل الدخول

للوصول إلى لوحة التحكم:

**الرابط**: `/login`

```
اسم المستخدم: admin
كلمة المرور: admin123
```

---

## 📱 صفحات الموقع

### الصفحات العامة (بدون تسجيل دخول)

| الرابط | الوصف |
|--------|-------|
| `/` | الصفحة الرئيسية |
| `/about` | صفحة من أنا |
| `/services` | صفحة الخدمات |
| `/contact` | صفحة اتصل بنا |

### صفحات الإدارة (تحتاج تسجيل دخول)

| الرابط | الوصف |
|--------|-------|
| `/login` | صفحة تسجيل الدخول |
| `/admin` | لوحة التحكم |

---

## 🎯 اختبار الموقع

### 1. الصفحة الرئيسية
- افتح الموقع وستشاهد صفحة ترحيبية باللغة العربية
- جرب الأزرار "احجز استشارة" و "خدماتنا"

### 2. صفحة الخدمات
- انقر على "خدماتنا" من القائمة
- ستشاهد 6 خدمات قانونية

### 3. تسجيل الدخول
- اذهب إلى `/login`
- أدخل:
  - اسم المستخدم: `admin`
  - كلمة المرور: `admin123`
- سيتم تحويلك تلقائياً إلى لوحة التحكم

### 4. لوحة التحكم
- تصفح التبويبات الثلاثة:
  - إدارة الخدمات (يمكنك حذف خدمات)
  - معلومات عني
  - معلومات الاتصال

---

## 💡 نصائح سريعة

### تعديل البيانات الافتراضية

البيانات موجودة في ملف:
```
lib/storage.ts
```

يمكنك تعديل:
- الخدمات الافتراضية
- معلومات "من أنا"
- معلومات الاتصال

### تغيير الألوان

الألوان موجودة في:
```
tailwind.config.js
```

الألوان الحالية:
- **Primary** (أزرق): للشريط والأزرار الرئيسية
- **Gold** (ذهبي): لأزرار CTA المهمة

### إضافة صفحات جديدة

أنشئ مجلد جديد في `app/`:
```bash
mkdir app/new-page
# ثم أضف ملف page.tsx
```

---

## 🔧 الأوامر المتاحة

```bash
# تشغيل الموقع
npm run dev

# بناء للإنتاج
npm run build

# تشغيل النسخة الإنتاجية
npm start

# فحص الأخطاء
npm run lint
```

---

## 📂 الملفات المهمة

| الملف | الوصف |
|-------|-------|
| `app/page.tsx` | الصفحة الرئيسية |
| `app/layout.tsx` | التخطيط العام |
| `components/Navbar.tsx` | شريط التنقل |
| `components/Footer.tsx` | التذييل |
| `lib/auth.ts` | نظام المصادقة |
| `lib/storage.ts` | نظام التخزين |
| `tailwind.config.js` | إعدادات التصميم |

---

## 🎨 تخصيص المحتوى

### 1. تغيير اسم المحامي

في `app/layout.tsx`:
```typescript
export const metadata: Metadata = {
  title: 'المحامي [اسمك هنا]',
  description: '...'
}
```

### 2. تغيير معلومات الاتصال

في `lib/storage.ts`، عدل:
```typescript
const defaultContact: ContactInfo = {
  phone: '[رقمك]',
  email: '[بريدك]',
  address: '[عنوانك]',
  workingHours: '[ساعات عملك]'
}
```

### 3. إضافة خدمة جديدة

في `lib/storage.ts`، أضف إلى `defaultServices`:
```typescript
{
  id: '7',
  title: 'اسم الخدمة',
  description: 'وصف الخدمة',
  icon: '📋' // أي emoji
}
```

---

## ✅ قائمة التحقق

- [ ] تشغيل المشروع بنجاح
- [ ] تصفح جميع الصفحات
- [ ] اختبار تسجيل الدخول
- [ ] الدخول إلى لوحة التحكم
- [ ] حذف خدمة من لوحة التحكم
- [ ] تعديل المحتوى حسب احتياجك

---

## 🆘 حل المشاكل الشائعة

### المشكلة: الموقع لا يعمل
**الحل**: تأكد من تثبيت الحزم:
```bash
npm install
```

### المشكلة: لا أستطيع تسجيل الدخول
**الحل**: تأكد من استخدام:
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

### المشكلة: البيانات اختفت
**الحل**: البيانات محفوظة في localStorage. امسح التخزين وأعد تحميل الصفحة لاستعادة البيانات الافتراضية.

---

## 📖 المزيد من المعلومات

راجع الملفات التالية:
- `README.md` - دليل شامل
- `PROJECT_SUMMARY.md` - ملخص تفصيلي للمشروع

---

## 🎉 استمتع!

الموقع جاهز للاستخدام. ابدأ بالتخصيص والتطوير حسب احتياجاتك!

**ملاحظة**: هذا إصدار تجريبي. للاستخدام في بيئة الإنتاج، تأكد من:
- استخدام قاعدة بيانات حقيقية
- تطبيق أمان قوي للمصادقة
- إضافة HTTPS
- اختبار شامل

---

**صنع بحب بواسطة Claude Code** ❤️
